{
  "name": "tensorcad",
  "private": true,
  "version": "0.0.0",
  "description": "Schematic capture for neural network architectures",
  "workspaces": [
    "packages/*",
    "desktop/frontend"
  ],
  "scripts": {
    "test": "bun test packages",
    "test:go": "cd packages/core-go && go test ./... && cd ../../desktop && go test ./...",
    "test:all": "bun run typecheck && bun test packages && bun run test:go",
    "golden": "bun run scripts/golden.ts",
    "typecheck": "tsc -p packages/core/tsconfig.json --noEmit",
    "cli": "bun run packages/cli/src/index.ts",
    "ui": "cd packages/ui && bun run dev",
    "test:core": "bun test packages/core/test",
    "report": "bun run scripts/report.ts",
    "codegen": "bun run scripts/codegen-demo.ts",
    "desktop": "cd desktop && wails3 dev",
    "desktop:build": "cd desktop && wails3 build"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
