{
  "preset": "deepseek-v3",
  "cases": [
    {
      "label": "default",
      "warnings": [],
      "model": "\"\"\"deepseek-v3 — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 61\n    D = 7168\n    H = 128\n    Hkv = 128\n    dh = 192\n    F = 18432\n    V = 129280\n    Ql = 1536\n    Kl = 512\n    dnope = 128\n    drope = 64\n    dv = 128\n    E = 256\n    K = 8\n    Fe = 2048\n    Ns = 1\n    Ld = 3\n    Lm = 58\n\nParameters: 671,026,419,200\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\n\n# Projections that write back into the residual stream. Their initial scale\n# is divided by sqrt(2 * layers) so depth does not inflate the stream.\nRESIDUAL_PROJECTIONS = (\"o_proj.weight\", \"down.weight\", \"out_proj.weight\")\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\nclass MlaAttention(nn.Module):\n    \"\"\"mla_attention: d_model=7168, heads=128, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, causal=true, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_down = nn.Linear(7168, 1536, bias=False)\n        self.q_norm = nn.RMSNorm(1536, eps=0.00001)\n        self.q_up = nn.Linear(1536, 24576, bias=False)\n        self.kv_down = nn.Linear(7168, 576, bias=False)\n        self.kv_norm = nn.RMSNorm(512, eps=0.00001)\n        self.k_up = nn.Linear(512, 16384, bias=False)\n        self.v_up = nn.Linear(512, 16384, bias=False)\n        self.k_rope = RotaryEmbedding(64, theta=10000)\n        self.o_proj = nn.Linear(16384, 7168, bias=False)\n\n    def forward(self, x):\n        q_down_y = self.q_down(x)\n        q_norm_y = self.q_norm(q_down_y)\n        q_up_y = self.q_up(q_norm_y)\n        B, T, _d2 = q_up_y.shape\n        q_heads_y = q_up_y.view(B, T, 128, 192).permute(0, 2, 1, 3)\n        kv_down_y = self.kv_down(x)\n        latent_y = kv_down_y  # cached latent\n        kv_split_y0, kv_split_y1 = torch.split(latent_y, [512, 64], dim=-1)\n        kv_norm_y = self.kv_norm(kv_split_y0)\n        k_up_y = self.k_up(kv_norm_y)\n        B, T, _d2 = k_up_y.shape\n        k_nope_heads_y = k_up_y.view(B, T, 128, 128).permute(0, 2, 1, 3)\n        v_up_y = self.v_up(kv_norm_y)\n        B, T, _d2 = v_up_y.shape\n        v_heads_y = v_up_y.view(B, T, 128, 128).permute(0, 2, 1, 3)\n        k_rope_shared_y = kv_split_y1.unsqueeze(1).expand(-1, 128, -1, -1)\n        k_rope_y = self.k_rope(k_rope_shared_y)\n        k_cat_y = torch.cat([k_nope_heads_y, k_rope_y], dim=-1)\n        attn_y = F.scaled_dot_product_attention(q_heads_y, k_cat_y, v_heads_y, is_causal=True, scale=0.0721687836487)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 16384)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass GatedMlp(nn.Module):\n    \"\"\"gated_mlp: d_model=7168, hidden=18432, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 18432, bias=False)\n        self.up = nn.Linear(7168, 18432, bias=False)\n        self.down = nn.Linear(18432, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=7168, heads=128, kv_heads=128, head_dim=192, ffn_hidden=18432, attention=mla, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(7168, eps=0.00001)\n        self.attn = MlaAttention()\n        self.norm2 = nn.RMSNorm(7168, eps=0.00001)\n        self.mlp = GatedMlp()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass Expert(nn.Module):\n    \"\"\"Expert: d_model=7168, hidden=2048, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 2048, bias=False)\n        self.up = nn.Linear(7168, 2048, bias=False)\n        self.down = nn.Linear(2048, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass SharedExpert(nn.Module):\n    \"\"\"SharedExpert: d_model=7168, hidden=2048, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 2048, bias=False)\n        self.up = nn.Linear(7168, 2048, bias=False)\n        self.down = nn.Linear(2048, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass MoeLayer(nn.Module):\n    \"\"\"256 experts, top 8 per token, plus 1 shared expert(s).\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.top_k = 8\n        self.router = nn.Linear(7168, 256, bias=True)\n        self.experts = nn.ModuleList([Expert() for _ in range(256)])\n        self.shared = SharedExpert()\n\n    def forward(self, x):\n        shape = x.shape\n        flat = x.reshape(-1, 7168)\n        scores = F.softmax(self.router(flat).float(), dim=-1)\n        weight, index = torch.topk(scores, self.top_k, dim=-1)\n        weight = weight / weight.sum(dim=-1, keepdim=True)\n        weight = weight.to(x.dtype)\n        out = torch.zeros_like(flat)\n        for e, expert in enumerate(self.experts):\n            rows, slot = (index == e).nonzero(as_tuple=True)\n            if rows.numel() == 0:\n                continue\n            out.index_add_(0, rows, expert(flat[rows]) * weight[rows, slot, None])\n        y = out.reshape(shape)\n        y = y + self.shared(x)\n        return y\n\nclass TransformerBlock2(nn.Module):\n    \"\"\"transformer_block: d_model=7168, heads=128, kv_heads=128, head_dim=192, ffn_hidden=18432, attention=mla, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=moe, experts=256, top_k=8, expert_hidden=2048, shared_experts=1, router_bias=true, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(7168, eps=0.00001)\n        self.attn = MlaAttention()\n        self.norm2 = nn.RMSNorm(7168, eps=0.00001)\n        self.mlp = MoeLayer()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer2(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock2()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass DeepseekV3(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(129280, 7168)\n        self.dense_layers = nn.ModuleList([Layer() for _ in range(3)])\n        self.layers = nn.ModuleList([Layer2() for _ in range(58)])\n        self.final_norm = nn.RMSNorm(7168, eps=0.00001)\n        self.head = nn.Linear(7168, 129280, bias=False)\n\n    @torch.no_grad()\n    def init_weights(self):\n        \"\"\"Normal(0, 0.02), with residual projections scaled by depth.\n\n        PyTorch's defaults leave nn.Embedding at a unit normal, which starts\n        a language model near a cross-entropy of a few hundred rather than\n        ln(vocab). Call this after moving the model to its device.\n        \"\"\"\n        for module in self.modules():\n            if isinstance(module, (nn.Linear, nn.Embedding)):\n                nn.init.normal_(module.weight, mean=0.0, std=0.02)\n                bias = getattr(module, \"bias\", None)\n                if bias is not None:\n                    nn.init.zeros_(bias)\n        for name, param in self.named_parameters():\n            if name.endswith(RESIDUAL_PROJECTIONS):\n                nn.init.normal_(param, mean=0.0, std=0.0018107149)\n        return self\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        dense_layers_x = embed_y\n        for layer in self.dense_layers:\n            dense_layers_x = layer(dense_layers_x)\n        layers_x = dense_layers_x\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        return head_y\n\n\nif __name__ == \"__main__\":\n    expected = 671026419200\n    with torch.device(\"meta\"):\n        model = DeepseekV3()\n    actual = sum(p.numel() for p in model.parameters())\n    print(f\"parameters: {actual:,} (design says {expected:,})\")\n    assert actual == expected, f\"parameter count differs: {actual} vs {expected}\"\n"
    },
    {
      "label": "dense",
      "warnings": [],
      "model": "\"\"\"deepseek-v3 — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 61\n    D = 7168\n    H = 128\n    Hkv = 128\n    dh = 192\n    F = 18432\n    V = 129280\n    Ql = 1536\n    Kl = 512\n    dnope = 128\n    drope = 64\n    dv = 128\n    E = 256\n    K = 8\n    Fe = 2048\n    Ns = 1\n    Ld = 3\n    Lm = 58\n\nParameters: 671,026,419,200\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\n\n# Projections that write back into the residual stream. Their initial scale\n# is divided by sqrt(2 * layers) so depth does not inflate the stream.\nRESIDUAL_PROJECTIONS = (\"o_proj.weight\", \"down.weight\", \"out_proj.weight\")\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\nclass MlaAttention(nn.Module):\n    \"\"\"mla_attention: d_model=7168, heads=128, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, causal=true, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_down = nn.Linear(7168, 1536, bias=False)\n        self.q_norm = nn.RMSNorm(1536, eps=0.00001)\n        self.q_up = nn.Linear(1536, 24576, bias=False)\n        self.kv_down = nn.Linear(7168, 576, bias=False)\n        self.kv_norm = nn.RMSNorm(512, eps=0.00001)\n        self.k_up = nn.Linear(512, 16384, bias=False)\n        self.v_up = nn.Linear(512, 16384, bias=False)\n        self.k_rope = RotaryEmbedding(64, theta=10000)\n        self.o_proj = nn.Linear(16384, 7168, bias=False)\n\n    def forward(self, x):\n        q_down_y = self.q_down(x)\n        q_norm_y = self.q_norm(q_down_y)\n        q_up_y = self.q_up(q_norm_y)\n        B, T, _d2 = q_up_y.shape\n        q_heads_y = q_up_y.view(B, T, 128, 192).permute(0, 2, 1, 3)\n        kv_down_y = self.kv_down(x)\n        latent_y = kv_down_y  # cached latent\n        kv_split_y0, kv_split_y1 = torch.split(latent_y, [512, 64], dim=-1)\n        kv_norm_y = self.kv_norm(kv_split_y0)\n        k_up_y = self.k_up(kv_norm_y)\n        B, T, _d2 = k_up_y.shape\n        k_nope_heads_y = k_up_y.view(B, T, 128, 128).permute(0, 2, 1, 3)\n        v_up_y = self.v_up(kv_norm_y)\n        B, T, _d2 = v_up_y.shape\n        v_heads_y = v_up_y.view(B, T, 128, 128).permute(0, 2, 1, 3)\n        k_rope_shared_y = kv_split_y1.unsqueeze(1).expand(-1, 128, -1, -1)\n        k_rope_y = self.k_rope(k_rope_shared_y)\n        k_cat_y = torch.cat([k_nope_heads_y, k_rope_y], dim=-1)\n        attn_y = F.scaled_dot_product_attention(q_heads_y, k_cat_y, v_heads_y, is_causal=True, scale=0.0721687836487)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 16384)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass GatedMlp(nn.Module):\n    \"\"\"gated_mlp: d_model=7168, hidden=18432, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 18432, bias=False)\n        self.up = nn.Linear(7168, 18432, bias=False)\n        self.down = nn.Linear(18432, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=7168, heads=128, kv_heads=128, head_dim=192, ffn_hidden=18432, attention=mla, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(7168, eps=0.00001)\n        self.attn = MlaAttention()\n        self.norm2 = nn.RMSNorm(7168, eps=0.00001)\n        self.mlp = GatedMlp()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass Expert(nn.Module):\n    \"\"\"Expert: d_model=7168, hidden=2048, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 2048, bias=False)\n        self.up = nn.Linear(7168, 2048, bias=False)\n        self.down = nn.Linear(2048, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass SharedExpert(nn.Module):\n    \"\"\"SharedExpert: d_model=7168, hidden=2048, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 2048, bias=False)\n        self.up = nn.Linear(7168, 2048, bias=False)\n        self.down = nn.Linear(2048, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass MoeLayer(nn.Module):\n    \"\"\"256 experts, top 8 per token, plus 1 shared expert(s). Dense dispatch: traceable, but 32x the work.\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.top_k = 8\n        self.router = nn.Linear(7168, 256, bias=True)\n        self.experts = nn.ModuleList([Expert() for _ in range(256)])\n        self.shared = SharedExpert()\n\n    def forward(self, x):\n        shape = x.shape\n        flat = x.reshape(-1, 7168)\n        scores = F.softmax(self.router(flat).float(), dim=-1)\n        weight, index = torch.topk(scores, self.top_k, dim=-1)\n        weight = weight / weight.sum(dim=-1, keepdim=True)\n        weight = weight.to(x.dtype)\n        gate = torch.zeros(flat.shape[0], 256, device=x.device, dtype=x.dtype)\n        gate = gate.scatter(1, index, weight)\n        out = torch.zeros_like(flat)\n        for e, expert in enumerate(self.experts):\n            out = out + expert(flat) * gate[:, e : e + 1]\n        y = out.reshape(shape)\n        y = y + self.shared(x)\n        return y\n\nclass TransformerBlock2(nn.Module):\n    \"\"\"transformer_block: d_model=7168, heads=128, kv_heads=128, head_dim=192, ffn_hidden=18432, attention=mla, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=moe, experts=256, top_k=8, expert_hidden=2048, shared_experts=1, router_bias=true, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(7168, eps=0.00001)\n        self.attn = MlaAttention()\n        self.norm2 = nn.RMSNorm(7168, eps=0.00001)\n        self.mlp = MoeLayer()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer2(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock2()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass DeepseekV3(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(129280, 7168)\n        self.dense_layers = nn.ModuleList([Layer() for _ in range(3)])\n        self.layers = nn.ModuleList([Layer2() for _ in range(58)])\n        self.final_norm = nn.RMSNorm(7168, eps=0.00001)\n        self.head = nn.Linear(7168, 129280, bias=False)\n\n    @torch.no_grad()\n    def init_weights(self):\n        \"\"\"Normal(0, 0.02), with residual projections scaled by depth.\n\n        PyTorch's defaults leave nn.Embedding at a unit normal, which starts\n        a language model near a cross-entropy of a few hundred rather than\n        ln(vocab). Call this after moving the model to its device.\n        \"\"\"\n        for module in self.modules():\n            if isinstance(module, (nn.Linear, nn.Embedding)):\n                nn.init.normal_(module.weight, mean=0.0, std=0.02)\n                bias = getattr(module, \"bias\", None)\n                if bias is not None:\n                    nn.init.zeros_(bias)\n        for name, param in self.named_parameters():\n            if name.endswith(RESIDUAL_PROJECTIONS):\n                nn.init.normal_(param, mean=0.0, std=0.0018107149)\n        return self\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        dense_layers_x = embed_y\n        for layer in self.dense_layers:\n            dense_layers_x = layer(dense_layers_x)\n        layers_x = dense_layers_x\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        return head_y\n\n\nif __name__ == \"__main__\":\n    expected = 671026419200\n    with torch.device(\"meta\"):\n        model = DeepseekV3()\n    actual = sum(p.numel() for p in model.parameters())\n    print(f\"parameters: {actual:,} (design says {expected:,})\")\n    assert actual == expected, f\"parameter count differs: {actual} vs {expected}\"\n"
    },
    {
      "label": "bare",
      "warnings": [],
      "model": "\"\"\"deepseek-v3 — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 61\n    D = 7168\n    H = 128\n    Hkv = 128\n    dh = 192\n    F = 18432\n    V = 129280\n    Ql = 1536\n    Kl = 512\n    dnope = 128\n    drope = 64\n    dv = 128\n    E = 256\n    K = 8\n    Fe = 2048\n    Ns = 1\n    Ld = 3\n    Lm = 58\n\nParameters: 671,026,419,200\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\nclass MlaAttention(nn.Module):\n    \"\"\"mla_attention: d_model=7168, heads=128, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, causal=true, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_down = nn.Linear(7168, 1536, bias=False)\n        self.q_norm = nn.RMSNorm(1536, eps=0.00001)\n        self.q_up = nn.Linear(1536, 24576, bias=False)\n        self.kv_down = nn.Linear(7168, 576, bias=False)\n        self.kv_norm = nn.RMSNorm(512, eps=0.00001)\n        self.k_up = nn.Linear(512, 16384, bias=False)\n        self.v_up = nn.Linear(512, 16384, bias=False)\n        self.k_rope = RotaryEmbedding(64, theta=10000)\n        self.o_proj = nn.Linear(16384, 7168, bias=False)\n\n    def forward(self, x):\n        q_down_y = self.q_down(x)\n        q_norm_y = self.q_norm(q_down_y)\n        q_up_y = self.q_up(q_norm_y)\n        B, T, _d2 = q_up_y.shape\n        q_heads_y = q_up_y.view(B, T, 128, 192).permute(0, 2, 1, 3)\n        kv_down_y = self.kv_down(x)\n        latent_y = kv_down_y  # cached latent\n        kv_split_y0, kv_split_y1 = torch.split(latent_y, [512, 64], dim=-1)\n        kv_norm_y = self.kv_norm(kv_split_y0)\n        k_up_y = self.k_up(kv_norm_y)\n        B, T, _d2 = k_up_y.shape\n        k_nope_heads_y = k_up_y.view(B, T, 128, 128).permute(0, 2, 1, 3)\n        v_up_y = self.v_up(kv_norm_y)\n        B, T, _d2 = v_up_y.shape\n        v_heads_y = v_up_y.view(B, T, 128, 128).permute(0, 2, 1, 3)\n        k_rope_shared_y = kv_split_y1.unsqueeze(1).expand(-1, 128, -1, -1)\n        k_rope_y = self.k_rope(k_rope_shared_y)\n        k_cat_y = torch.cat([k_nope_heads_y, k_rope_y], dim=-1)\n        attn_y = F.scaled_dot_product_attention(q_heads_y, k_cat_y, v_heads_y, is_causal=True, scale=0.0721687836487)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 16384)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass GatedMlp(nn.Module):\n    \"\"\"gated_mlp: d_model=7168, hidden=18432, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 18432, bias=False)\n        self.up = nn.Linear(7168, 18432, bias=False)\n        self.down = nn.Linear(18432, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=7168, heads=128, kv_heads=128, head_dim=192, ffn_hidden=18432, attention=mla, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(7168, eps=0.00001)\n        self.attn = MlaAttention()\n        self.norm2 = nn.RMSNorm(7168, eps=0.00001)\n        self.mlp = GatedMlp()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass Expert(nn.Module):\n    \"\"\"Expert: d_model=7168, hidden=2048, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 2048, bias=False)\n        self.up = nn.Linear(7168, 2048, bias=False)\n        self.down = nn.Linear(2048, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass SharedExpert(nn.Module):\n    \"\"\"SharedExpert: d_model=7168, hidden=2048, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(7168, 2048, bias=False)\n        self.up = nn.Linear(7168, 2048, bias=False)\n        self.down = nn.Linear(2048, 7168, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass MoeLayer(nn.Module):\n    \"\"\"256 experts, top 8 per token, plus 1 shared expert(s).\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.top_k = 8\n        self.router = nn.Linear(7168, 256, bias=True)\n        self.experts = nn.ModuleList([Expert() for _ in range(256)])\n        self.shared = SharedExpert()\n\n    def forward(self, x):\n        shape = x.shape\n        flat = x.reshape(-1, 7168)\n        scores = F.softmax(self.router(flat).float(), dim=-1)\n        weight, index = torch.topk(scores, self.top_k, dim=-1)\n        weight = weight / weight.sum(dim=-1, keepdim=True)\n        weight = weight.to(x.dtype)\n        out = torch.zeros_like(flat)\n        for e, expert in enumerate(self.experts):\n            rows, slot = (index == e).nonzero(as_tuple=True)\n            if rows.numel() == 0:\n                continue\n            out.index_add_(0, rows, expert(flat[rows]) * weight[rows, slot, None])\n        y = out.reshape(shape)\n        y = y + self.shared(x)\n        return y\n\nclass TransformerBlock2(nn.Module):\n    \"\"\"transformer_block: d_model=7168, heads=128, kv_heads=128, head_dim=192, ffn_hidden=18432, attention=mla, q_lora=1536, kv_lora=512, nope_dim=128, rope_dim=64, v_dim=128, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=moe, experts=256, top_k=8, expert_hidden=2048, shared_experts=1, router_bias=true, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(7168, eps=0.00001)\n        self.attn = MlaAttention()\n        self.norm2 = nn.RMSNorm(7168, eps=0.00001)\n        self.mlp = MoeLayer()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer2(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock2()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass Net(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(129280, 7168)\n        self.dense_layers = nn.ModuleList([Layer() for _ in range(3)])\n        self.layers = nn.ModuleList([Layer2() for _ in range(58)])\n        self.final_norm = nn.RMSNorm(7168, eps=0.00001)\n        self.head = nn.Linear(7168, 129280, bias=False)\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        dense_layers_x = embed_y\n        for layer in self.dense_layers:\n            dense_layers_x = layer(dense_layers_x)\n        layers_x = dense_layers_x\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        return head_y\n"
    }
  ]
}
