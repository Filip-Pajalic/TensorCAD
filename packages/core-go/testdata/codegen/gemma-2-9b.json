{
  "preset": "gemma-2-9b",
  "cases": [
    {
      "label": "default",
      "options": {},
      "warnings": [],
      "model": "\"\"\"gemma-2-9b — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 42\n    W = 4096\n    D = 3584\n    H = 16\n    Hkv = 8\n    dh = 256\n    F = 14336\n    V = 256000\n\nParameters: 9,241,705,984\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\n\n# Projections that write back into the residual stream. Their initial scale\n# is divided by sqrt(2 * layers) so depth does not inflate the stream.\nRESIDUAL_PROJECTIONS = (\"o_proj.weight\", \"down.weight\", \"out_proj.weight\")\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\ndef sliding_window_mask(seq: int, window: int, device, dtype) -> torch.Tensor:\n    \"\"\"Causal mask that also forbids attending further back than `window` tokens.\"\"\"\n    i = torch.arange(seq, device=device)\n    allowed = (i[:, None] >= i[None, :]) & (i[:, None] - i[None, :] < window)\n    mask = torch.zeros(seq, seq, device=device, dtype=dtype)\n    return mask.masked_fill(~allowed, float(\"-inf\"))\n\n\ndef causal_mask(seq: int, device, dtype) -> torch.Tensor:\n    \"\"\"Additive mask forbidding a token from attending to anything after it.\"\"\"\n    i = torch.arange(seq, device=device)\n    mask = torch.zeros(seq, seq, device=device, dtype=dtype)\n    return mask.masked_fill(i[:, None] < i[None, :], float(\"-inf\"))\n\n\ndef softcap_attention(q, k, v, cap, mask=None, scale=None, enable_gqa=False):\n    \"\"\"Attention whose scores are bounded to +/-cap by a tanh.\n\n    Not `F.scaled_dot_product_attention`: the cap applies to the score matrix,\n    and a fused kernel never materializes one. This is the eager form, and it\n    costs the memory the fused kernel would have saved.\n    \"\"\"\n    if enable_gqa and k.shape[-3] != q.shape[-3]:\n        k = k.repeat_interleave(q.shape[-3] // k.shape[-3], dim=-3)\n        v = v.repeat_interleave(q.shape[-3] // v.shape[-3], dim=-3)\n    if scale is None:\n        scale = q.shape[-1] ** -0.5\n    scores = torch.tanh((q @ k.transpose(-2, -1)) * scale / cap) * cap\n    if mask is not None:\n        scores = scores + mask\n    return torch.softmax(scores, dim=-1).to(v.dtype) @ v\n\n\nclass GqaAttention(nn.Module):\n    \"\"\"gqa_attention: d_model=3584, heads=16, kv_heads=8, head_dim=256, bias=false, causal=true, window=4096, qk_norm=false, flash=true, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(3584, 4096, bias=False)\n        self.k_proj = nn.Linear(3584, 2048, bias=False)\n        self.v_proj = nn.Linear(3584, 2048, bias=False)\n        self.rope_q = RotaryEmbedding(256, theta=10000)\n        self.rope_k = RotaryEmbedding(256, theta=10000)\n        self.o_proj = nn.Linear(4096, 3584, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 16, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y_mask = sliding_window_mask(rope_q_y.shape[-2], 4096, rope_q_y.device, rope_q_y.dtype)\n        attn_y = softcap_attention(rope_q_y, rope_k_y, v_heads_y, 50, mask=attn_y_mask, scale=None, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass GatedMlp(nn.Module):\n    \"\"\"gated_mlp: d_model=3584, hidden=14336, act=gelu_tanh, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(3584, 14336, bias=False)\n        self.up = nn.Linear(3584, 14336, bias=False)\n        self.down = nn.Linear(14336, 3584, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.gelu(gate_y, approximate=\"tanh\")\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=3584, heads=16, kv_heads=8, head_dim=256, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=true, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=gelu_tanh, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=4096, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(3584, eps=0.00001)\n        self.attn = GqaAttention()\n        self.post_attn_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.norm2 = nn.RMSNorm(3584, eps=0.00001)\n        self.mlp = GatedMlp()\n        self.post_mlp_norm = nn.RMSNorm(3584, eps=0.00001)\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        post_attn_norm_y = self.post_attn_norm(attn_y)\n        resid1_y = post_attn_norm_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        post_mlp_norm_y = self.post_mlp_norm(mlp_y)\n        resid2_y = post_mlp_norm_y + resid1_y\n        return resid2_y\n\nclass GqaAttention2(nn.Module):\n    \"\"\"gqa_attention: d_model=3584, heads=16, kv_heads=8, head_dim=256, bias=false, causal=true, window=0, qk_norm=false, flash=true, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(3584, 4096, bias=False)\n        self.k_proj = nn.Linear(3584, 2048, bias=False)\n        self.v_proj = nn.Linear(3584, 2048, bias=False)\n        self.rope_q = RotaryEmbedding(256, theta=10000)\n        self.rope_k = RotaryEmbedding(256, theta=10000)\n        self.o_proj = nn.Linear(4096, 3584, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 16, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y_mask = causal_mask(rope_q_y.shape[-2], rope_q_y.device, rope_q_y.dtype)\n        attn_y = softcap_attention(rope_q_y, rope_k_y, v_heads_y, 50, mask=attn_y_mask, scale=None, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass TransformerBlock2(nn.Module):\n    \"\"\"transformer_block: d_model=3584, heads=16, kv_heads=8, head_dim=256, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=true, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=gelu_tanh, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(3584, eps=0.00001)\n        self.attn = GqaAttention2()\n        self.post_attn_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.norm2 = nn.RMSNorm(3584, eps=0.00001)\n        self.mlp = GatedMlp()\n        self.post_mlp_norm = nn.RMSNorm(3584, eps=0.00001)\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        post_attn_norm_y = self.post_attn_norm(attn_y)\n        resid1_y = post_attn_norm_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        post_mlp_norm_y = self.post_mlp_norm(mlp_y)\n        resid2_y = post_mlp_norm_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.local = TransformerBlock()\n        self.global_ = TransformerBlock2()\n\n    def forward(self, x):\n        local_y = self.local(x)\n        global_y = self.global_(local_y)\n        return global_y\n\nclass Gemma29b(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(256000, 3584)\n        self.layers = nn.ModuleList([Layer() for _ in range(21)])\n        self.final_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.head = nn.Linear(3584, 256000, bias=False)\n        self.head.weight = self.embed.weight\n\n    @torch.no_grad()\n    def init_weights(self):\n        \"\"\"Normal(0, 0.02), with residual projections scaled by depth.\n\n        PyTorch's defaults leave nn.Embedding at a unit normal, which starts\n        a language model near a cross-entropy of a few hundred rather than\n        ln(vocab). Call this after moving the model to its device.\n        \"\"\"\n        for module in self.modules():\n            if isinstance(module, (nn.Linear, nn.Embedding)):\n                nn.init.normal_(module.weight, mean=0.0, std=0.02)\n                bias = getattr(module, \"bias\", None)\n                if bias is not None:\n                    nn.init.zeros_(bias)\n        for name, param in self.named_parameters():\n            if name.endswith(RESIDUAL_PROJECTIONS):\n                nn.init.normal_(param, mean=0.0, std=0.0030860670)\n        return self\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        layers_x = embed_y\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        head_y = torch.tanh(head_y / 30) * 30\n        return head_y\n\n\nif __name__ == \"__main__\":\n    expected = 9241705984\n    with torch.device(\"meta\"):\n        model = Gemma29b()\n    actual = sum(p.numel() for p in model.parameters())\n    print(f\"parameters: {actual:,} (design says {expected:,})\")\n    assert actual == expected, f\"parameter count differs: {actual} vs {expected}\"\n"
    },
    {
      "label": "dense",
      "options": {
        "moeDispatch": "dense"
      },
      "warnings": [],
      "model": "\"\"\"gemma-2-9b — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 42\n    W = 4096\n    D = 3584\n    H = 16\n    Hkv = 8\n    dh = 256\n    F = 14336\n    V = 256000\n\nParameters: 9,241,705,984\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\n\n# Projections that write back into the residual stream. Their initial scale\n# is divided by sqrt(2 * layers) so depth does not inflate the stream.\nRESIDUAL_PROJECTIONS = (\"o_proj.weight\", \"down.weight\", \"out_proj.weight\")\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\ndef sliding_window_mask(seq: int, window: int, device, dtype) -> torch.Tensor:\n    \"\"\"Causal mask that also forbids attending further back than `window` tokens.\"\"\"\n    i = torch.arange(seq, device=device)\n    allowed = (i[:, None] >= i[None, :]) & (i[:, None] - i[None, :] < window)\n    mask = torch.zeros(seq, seq, device=device, dtype=dtype)\n    return mask.masked_fill(~allowed, float(\"-inf\"))\n\n\ndef causal_mask(seq: int, device, dtype) -> torch.Tensor:\n    \"\"\"Additive mask forbidding a token from attending to anything after it.\"\"\"\n    i = torch.arange(seq, device=device)\n    mask = torch.zeros(seq, seq, device=device, dtype=dtype)\n    return mask.masked_fill(i[:, None] < i[None, :], float(\"-inf\"))\n\n\ndef softcap_attention(q, k, v, cap, mask=None, scale=None, enable_gqa=False):\n    \"\"\"Attention whose scores are bounded to +/-cap by a tanh.\n\n    Not `F.scaled_dot_product_attention`: the cap applies to the score matrix,\n    and a fused kernel never materializes one. This is the eager form, and it\n    costs the memory the fused kernel would have saved.\n    \"\"\"\n    if enable_gqa and k.shape[-3] != q.shape[-3]:\n        k = k.repeat_interleave(q.shape[-3] // k.shape[-3], dim=-3)\n        v = v.repeat_interleave(q.shape[-3] // v.shape[-3], dim=-3)\n    if scale is None:\n        scale = q.shape[-1] ** -0.5\n    scores = torch.tanh((q @ k.transpose(-2, -1)) * scale / cap) * cap\n    if mask is not None:\n        scores = scores + mask\n    return torch.softmax(scores, dim=-1).to(v.dtype) @ v\n\n\nclass GqaAttention(nn.Module):\n    \"\"\"gqa_attention: d_model=3584, heads=16, kv_heads=8, head_dim=256, bias=false, causal=true, window=4096, qk_norm=false, flash=true, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(3584, 4096, bias=False)\n        self.k_proj = nn.Linear(3584, 2048, bias=False)\n        self.v_proj = nn.Linear(3584, 2048, bias=False)\n        self.rope_q = RotaryEmbedding(256, theta=10000)\n        self.rope_k = RotaryEmbedding(256, theta=10000)\n        self.o_proj = nn.Linear(4096, 3584, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 16, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y_mask = sliding_window_mask(rope_q_y.shape[-2], 4096, rope_q_y.device, rope_q_y.dtype)\n        attn_y = softcap_attention(rope_q_y, rope_k_y, v_heads_y, 50, mask=attn_y_mask, scale=None, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass GatedMlp(nn.Module):\n    \"\"\"gated_mlp: d_model=3584, hidden=14336, act=gelu_tanh, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(3584, 14336, bias=False)\n        self.up = nn.Linear(3584, 14336, bias=False)\n        self.down = nn.Linear(14336, 3584, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.gelu(gate_y, approximate=\"tanh\")\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=3584, heads=16, kv_heads=8, head_dim=256, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=true, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=gelu_tanh, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=4096, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(3584, eps=0.00001)\n        self.attn = GqaAttention()\n        self.post_attn_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.norm2 = nn.RMSNorm(3584, eps=0.00001)\n        self.mlp = GatedMlp()\n        self.post_mlp_norm = nn.RMSNorm(3584, eps=0.00001)\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        post_attn_norm_y = self.post_attn_norm(attn_y)\n        resid1_y = post_attn_norm_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        post_mlp_norm_y = self.post_mlp_norm(mlp_y)\n        resid2_y = post_mlp_norm_y + resid1_y\n        return resid2_y\n\nclass GqaAttention2(nn.Module):\n    \"\"\"gqa_attention: d_model=3584, heads=16, kv_heads=8, head_dim=256, bias=false, causal=true, window=0, qk_norm=false, flash=true, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(3584, 4096, bias=False)\n        self.k_proj = nn.Linear(3584, 2048, bias=False)\n        self.v_proj = nn.Linear(3584, 2048, bias=False)\n        self.rope_q = RotaryEmbedding(256, theta=10000)\n        self.rope_k = RotaryEmbedding(256, theta=10000)\n        self.o_proj = nn.Linear(4096, 3584, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 16, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y_mask = causal_mask(rope_q_y.shape[-2], rope_q_y.device, rope_q_y.dtype)\n        attn_y = softcap_attention(rope_q_y, rope_k_y, v_heads_y, 50, mask=attn_y_mask, scale=None, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass TransformerBlock2(nn.Module):\n    \"\"\"transformer_block: d_model=3584, heads=16, kv_heads=8, head_dim=256, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=true, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=gelu_tanh, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(3584, eps=0.00001)\n        self.attn = GqaAttention2()\n        self.post_attn_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.norm2 = nn.RMSNorm(3584, eps=0.00001)\n        self.mlp = GatedMlp()\n        self.post_mlp_norm = nn.RMSNorm(3584, eps=0.00001)\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        post_attn_norm_y = self.post_attn_norm(attn_y)\n        resid1_y = post_attn_norm_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        post_mlp_norm_y = self.post_mlp_norm(mlp_y)\n        resid2_y = post_mlp_norm_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.local = TransformerBlock()\n        self.global_ = TransformerBlock2()\n\n    def forward(self, x):\n        local_y = self.local(x)\n        global_y = self.global_(local_y)\n        return global_y\n\nclass Gemma29b(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(256000, 3584)\n        self.layers = nn.ModuleList([Layer() for _ in range(21)])\n        self.final_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.head = nn.Linear(3584, 256000, bias=False)\n        self.head.weight = self.embed.weight\n\n    @torch.no_grad()\n    def init_weights(self):\n        \"\"\"Normal(0, 0.02), with residual projections scaled by depth.\n\n        PyTorch's defaults leave nn.Embedding at a unit normal, which starts\n        a language model near a cross-entropy of a few hundred rather than\n        ln(vocab). Call this after moving the model to its device.\n        \"\"\"\n        for module in self.modules():\n            if isinstance(module, (nn.Linear, nn.Embedding)):\n                nn.init.normal_(module.weight, mean=0.0, std=0.02)\n                bias = getattr(module, \"bias\", None)\n                if bias is not None:\n                    nn.init.zeros_(bias)\n        for name, param in self.named_parameters():\n            if name.endswith(RESIDUAL_PROJECTIONS):\n                nn.init.normal_(param, mean=0.0, std=0.0030860670)\n        return self\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        layers_x = embed_y\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        head_y = torch.tanh(head_y / 30) * 30\n        return head_y\n\n\nif __name__ == \"__main__\":\n    expected = 9241705984\n    with torch.device(\"meta\"):\n        model = Gemma29b()\n    actual = sum(p.numel() for p in model.parameters())\n    print(f\"parameters: {actual:,} (design says {expected:,})\")\n    assert actual == expected, f\"parameter count differs: {actual} vs {expected}\"\n"
    },
    {
      "label": "bare",
      "options": {
        "className": "Net",
        "includeSmokeTest": false,
        "initStd": 0
      },
      "warnings": [],
      "model": "\"\"\"gemma-2-9b — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 42\n    W = 4096\n    D = 3584\n    H = 16\n    Hkv = 8\n    dh = 256\n    F = 14336\n    V = 256000\n\nParameters: 9,241,705,984\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\ndef sliding_window_mask(seq: int, window: int, device, dtype) -> torch.Tensor:\n    \"\"\"Causal mask that also forbids attending further back than `window` tokens.\"\"\"\n    i = torch.arange(seq, device=device)\n    allowed = (i[:, None] >= i[None, :]) & (i[:, None] - i[None, :] < window)\n    mask = torch.zeros(seq, seq, device=device, dtype=dtype)\n    return mask.masked_fill(~allowed, float(\"-inf\"))\n\n\ndef causal_mask(seq: int, device, dtype) -> torch.Tensor:\n    \"\"\"Additive mask forbidding a token from attending to anything after it.\"\"\"\n    i = torch.arange(seq, device=device)\n    mask = torch.zeros(seq, seq, device=device, dtype=dtype)\n    return mask.masked_fill(i[:, None] < i[None, :], float(\"-inf\"))\n\n\ndef softcap_attention(q, k, v, cap, mask=None, scale=None, enable_gqa=False):\n    \"\"\"Attention whose scores are bounded to +/-cap by a tanh.\n\n    Not `F.scaled_dot_product_attention`: the cap applies to the score matrix,\n    and a fused kernel never materializes one. This is the eager form, and it\n    costs the memory the fused kernel would have saved.\n    \"\"\"\n    if enable_gqa and k.shape[-3] != q.shape[-3]:\n        k = k.repeat_interleave(q.shape[-3] // k.shape[-3], dim=-3)\n        v = v.repeat_interleave(q.shape[-3] // v.shape[-3], dim=-3)\n    if scale is None:\n        scale = q.shape[-1] ** -0.5\n    scores = torch.tanh((q @ k.transpose(-2, -1)) * scale / cap) * cap\n    if mask is not None:\n        scores = scores + mask\n    return torch.softmax(scores, dim=-1).to(v.dtype) @ v\n\n\nclass GqaAttention(nn.Module):\n    \"\"\"gqa_attention: d_model=3584, heads=16, kv_heads=8, head_dim=256, bias=false, causal=true, window=4096, qk_norm=false, flash=true, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(3584, 4096, bias=False)\n        self.k_proj = nn.Linear(3584, 2048, bias=False)\n        self.v_proj = nn.Linear(3584, 2048, bias=False)\n        self.rope_q = RotaryEmbedding(256, theta=10000)\n        self.rope_k = RotaryEmbedding(256, theta=10000)\n        self.o_proj = nn.Linear(4096, 3584, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 16, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y_mask = sliding_window_mask(rope_q_y.shape[-2], 4096, rope_q_y.device, rope_q_y.dtype)\n        attn_y = softcap_attention(rope_q_y, rope_k_y, v_heads_y, 50, mask=attn_y_mask, scale=None, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass GatedMlp(nn.Module):\n    \"\"\"gated_mlp: d_model=3584, hidden=14336, act=gelu_tanh, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(3584, 14336, bias=False)\n        self.up = nn.Linear(3584, 14336, bias=False)\n        self.down = nn.Linear(14336, 3584, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.gelu(gate_y, approximate=\"tanh\")\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=3584, heads=16, kv_heads=8, head_dim=256, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=true, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=gelu_tanh, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=4096, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(3584, eps=0.00001)\n        self.attn = GqaAttention()\n        self.post_attn_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.norm2 = nn.RMSNorm(3584, eps=0.00001)\n        self.mlp = GatedMlp()\n        self.post_mlp_norm = nn.RMSNorm(3584, eps=0.00001)\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        post_attn_norm_y = self.post_attn_norm(attn_y)\n        resid1_y = post_attn_norm_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        post_mlp_norm_y = self.post_mlp_norm(mlp_y)\n        resid2_y = post_mlp_norm_y + resid1_y\n        return resid2_y\n\nclass GqaAttention2(nn.Module):\n    \"\"\"gqa_attention: d_model=3584, heads=16, kv_heads=8, head_dim=256, bias=false, causal=true, window=0, qk_norm=false, flash=true, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(3584, 4096, bias=False)\n        self.k_proj = nn.Linear(3584, 2048, bias=False)\n        self.v_proj = nn.Linear(3584, 2048, bias=False)\n        self.rope_q = RotaryEmbedding(256, theta=10000)\n        self.rope_k = RotaryEmbedding(256, theta=10000)\n        self.o_proj = nn.Linear(4096, 3584, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 16, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 256).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y_mask = causal_mask(rope_q_y.shape[-2], rope_q_y.device, rope_q_y.dtype)\n        attn_y = softcap_attention(rope_q_y, rope_k_y, v_heads_y, 50, mask=attn_y_mask, scale=None, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass TransformerBlock2(nn.Module):\n    \"\"\"transformer_block: d_model=3584, heads=16, kv_heads=8, head_dim=256, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=true, mlp=gated, experts=0, top_k=1, expert_hidden=0, shared_experts=0, router_bias=false, act=gelu_tanh, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0, logit_softcap=50, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(3584, eps=0.00001)\n        self.attn = GqaAttention2()\n        self.post_attn_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.norm2 = nn.RMSNorm(3584, eps=0.00001)\n        self.mlp = GatedMlp()\n        self.post_mlp_norm = nn.RMSNorm(3584, eps=0.00001)\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        post_attn_norm_y = self.post_attn_norm(attn_y)\n        resid1_y = post_attn_norm_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        post_mlp_norm_y = self.post_mlp_norm(mlp_y)\n        resid2_y = post_mlp_norm_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.local = TransformerBlock()\n        self.global_ = TransformerBlock2()\n\n    def forward(self, x):\n        local_y = self.local(x)\n        global_y = self.global_(local_y)\n        return global_y\n\nclass Net(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(256000, 3584)\n        self.layers = nn.ModuleList([Layer() for _ in range(21)])\n        self.final_norm = nn.RMSNorm(3584, eps=0.00001)\n        self.head = nn.Linear(3584, 256000, bias=False)\n        self.head.weight = self.embed.weight\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        layers_x = embed_y\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        head_y = torch.tanh(head_y / 30) * 30\n        return head_y\n"
    }
  ]
}
