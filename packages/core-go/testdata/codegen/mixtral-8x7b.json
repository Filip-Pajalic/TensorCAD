{
  "preset": "mixtral-8x7b",
  "cases": [
    {
      "label": "default",
      "options": {},
      "warnings": [],
      "model": "\"\"\"mixtral-8x7b — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 32\n    D = 4096\n    H = 32\n    Hkv = 8\n    dh = 128\n    F = 14336\n    V = 32000\n    E = 8\n    K = 2\n    Fe = 14336\n\nParameters: 46,702,792,704\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\n\n# Projections that write back into the residual stream. Their initial scale\n# is divided by sqrt(2 * layers) so depth does not inflate the stream.\nRESIDUAL_PROJECTIONS = (\"o_proj.weight\", \"down.weight\", \"out_proj.weight\")\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\nclass GqaAttention(nn.Module):\n    \"\"\"gqa_attention: d_model=4096, heads=32, kv_heads=8, head_dim=128, bias=false, causal=true, window=0, qk_norm=false, flash=true, logit_softcap=0, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(4096, 4096, bias=False)\n        self.k_proj = nn.Linear(4096, 1024, bias=False)\n        self.v_proj = nn.Linear(4096, 1024, bias=False)\n        self.rope_q = RotaryEmbedding(128, theta=1000000)\n        self.rope_k = RotaryEmbedding(128, theta=1000000)\n        self.o_proj = nn.Linear(4096, 4096, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 32, 128).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 128).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 128).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y = F.scaled_dot_product_attention(rope_q_y, rope_k_y, v_heads_y, is_causal=True, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass Expert(nn.Module):\n    \"\"\"Expert: d_model=4096, hidden=14336, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(4096, 14336, bias=False)\n        self.up = nn.Linear(4096, 14336, bias=False)\n        self.down = nn.Linear(14336, 4096, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass MoeLayer(nn.Module):\n    \"\"\"8 experts, top 2 per token.\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.top_k = 2\n        self.router = nn.Linear(4096, 8, bias=False)\n        self.experts = nn.ModuleList([Expert() for _ in range(8)])\n\n    def forward(self, x):\n        shape = x.shape\n        flat = x.reshape(-1, 4096)\n        scores = F.softmax(self.router(flat).float(), dim=-1)\n        weight, index = torch.topk(scores, self.top_k, dim=-1)\n        weight = weight / weight.sum(dim=-1, keepdim=True)\n        weight = weight.to(x.dtype)\n        out = torch.zeros_like(flat)\n        for e, expert in enumerate(self.experts):\n            rows, slot = (index == e).nonzero(as_tuple=True)\n            if rows.numel() == 0:\n                continue\n            out.index_add_(0, rows, expert(flat[rows]) * weight[rows, slot, None])\n        y = out.reshape(shape)\n        return y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=4096, heads=32, kv_heads=8, head_dim=128, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=moe, experts=8, top_k=2, expert_hidden=14336, shared_experts=0, router_bias=false, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0, logit_softcap=0, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(4096, eps=0.00001)\n        self.attn = GqaAttention()\n        self.norm2 = nn.RMSNorm(4096, eps=0.00001)\n        self.mlp = MoeLayer()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass Mixtral8x7b(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(32000, 4096)\n        self.layers = nn.ModuleList([Layer() for _ in range(32)])\n        self.final_norm = nn.RMSNorm(4096, eps=0.00001)\n        self.head = nn.Linear(4096, 32000, bias=False)\n\n    @torch.no_grad()\n    def init_weights(self):\n        \"\"\"Normal(0, 0.02), with residual projections scaled by depth.\n\n        PyTorch's defaults leave nn.Embedding at a unit normal, which starts\n        a language model near a cross-entropy of a few hundred rather than\n        ln(vocab). Call this after moving the model to its device.\n        \"\"\"\n        for module in self.modules():\n            if isinstance(module, (nn.Linear, nn.Embedding)):\n                nn.init.normal_(module.weight, mean=0.0, std=0.02)\n                bias = getattr(module, \"bias\", None)\n                if bias is not None:\n                    nn.init.zeros_(bias)\n        for name, param in self.named_parameters():\n            if name.endswith(RESIDUAL_PROJECTIONS):\n                nn.init.normal_(param, mean=0.0, std=0.0025000000)\n        return self\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        layers_x = embed_y\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        return head_y\n\n\nif __name__ == \"__main__\":\n    expected = 46702792704\n    with torch.device(\"meta\"):\n        model = Mixtral8x7b()\n    actual = sum(p.numel() for p in model.parameters())\n    print(f\"parameters: {actual:,} (design says {expected:,})\")\n    assert actual == expected, f\"parameter count differs: {actual} vs {expected}\"\n"
    },
    {
      "label": "dense",
      "options": {
        "moeDispatch": "dense"
      },
      "warnings": [],
      "model": "\"\"\"mixtral-8x7b — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 32\n    D = 4096\n    H = 32\n    Hkv = 8\n    dh = 128\n    F = 14336\n    V = 32000\n    E = 8\n    K = 2\n    Fe = 14336\n\nParameters: 46,702,792,704\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\n\n# Projections that write back into the residual stream. Their initial scale\n# is divided by sqrt(2 * layers) so depth does not inflate the stream.\nRESIDUAL_PROJECTIONS = (\"o_proj.weight\", \"down.weight\", \"out_proj.weight\")\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\nclass GqaAttention(nn.Module):\n    \"\"\"gqa_attention: d_model=4096, heads=32, kv_heads=8, head_dim=128, bias=false, causal=true, window=0, qk_norm=false, flash=true, logit_softcap=0, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(4096, 4096, bias=False)\n        self.k_proj = nn.Linear(4096, 1024, bias=False)\n        self.v_proj = nn.Linear(4096, 1024, bias=False)\n        self.rope_q = RotaryEmbedding(128, theta=1000000)\n        self.rope_k = RotaryEmbedding(128, theta=1000000)\n        self.o_proj = nn.Linear(4096, 4096, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 32, 128).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 128).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 128).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y = F.scaled_dot_product_attention(rope_q_y, rope_k_y, v_heads_y, is_causal=True, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass Expert(nn.Module):\n    \"\"\"Expert: d_model=4096, hidden=14336, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(4096, 14336, bias=False)\n        self.up = nn.Linear(4096, 14336, bias=False)\n        self.down = nn.Linear(14336, 4096, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass MoeLayer(nn.Module):\n    \"\"\"8 experts, top 2 per token. Dense dispatch: traceable, but 4x the work.\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.top_k = 2\n        self.router = nn.Linear(4096, 8, bias=False)\n        self.experts = nn.ModuleList([Expert() for _ in range(8)])\n\n    def forward(self, x):\n        shape = x.shape\n        flat = x.reshape(-1, 4096)\n        scores = F.softmax(self.router(flat).float(), dim=-1)\n        weight, index = torch.topk(scores, self.top_k, dim=-1)\n        weight = weight / weight.sum(dim=-1, keepdim=True)\n        weight = weight.to(x.dtype)\n        gate = torch.zeros(flat.shape[0], 8, device=x.device, dtype=x.dtype)\n        gate = gate.scatter(1, index, weight)\n        out = torch.zeros_like(flat)\n        for e, expert in enumerate(self.experts):\n            out = out + expert(flat) * gate[:, e : e + 1]\n        y = out.reshape(shape)\n        return y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=4096, heads=32, kv_heads=8, head_dim=128, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=moe, experts=8, top_k=2, expert_hidden=14336, shared_experts=0, router_bias=false, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0, logit_softcap=0, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(4096, eps=0.00001)\n        self.attn = GqaAttention()\n        self.norm2 = nn.RMSNorm(4096, eps=0.00001)\n        self.mlp = MoeLayer()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass Mixtral8x7b(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(32000, 4096)\n        self.layers = nn.ModuleList([Layer() for _ in range(32)])\n        self.final_norm = nn.RMSNorm(4096, eps=0.00001)\n        self.head = nn.Linear(4096, 32000, bias=False)\n\n    @torch.no_grad()\n    def init_weights(self):\n        \"\"\"Normal(0, 0.02), with residual projections scaled by depth.\n\n        PyTorch's defaults leave nn.Embedding at a unit normal, which starts\n        a language model near a cross-entropy of a few hundred rather than\n        ln(vocab). Call this after moving the model to its device.\n        \"\"\"\n        for module in self.modules():\n            if isinstance(module, (nn.Linear, nn.Embedding)):\n                nn.init.normal_(module.weight, mean=0.0, std=0.02)\n                bias = getattr(module, \"bias\", None)\n                if bias is not None:\n                    nn.init.zeros_(bias)\n        for name, param in self.named_parameters():\n            if name.endswith(RESIDUAL_PROJECTIONS):\n                nn.init.normal_(param, mean=0.0, std=0.0025000000)\n        return self\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        layers_x = embed_y\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        return head_y\n\n\nif __name__ == \"__main__\":\n    expected = 46702792704\n    with torch.device(\"meta\"):\n        model = Mixtral8x7b()\n    actual = sum(p.numel() for p in model.parameters())\n    print(f\"parameters: {actual:,} (design says {expected:,})\")\n    assert actual == expected, f\"parameter count differs: {actual} vs {expected}\"\n"
    },
    {
      "label": "bare",
      "options": {
        "className": "Net",
        "includeSmokeTest": false,
        "initStd": 0
      },
      "warnings": [],
      "model": "\"\"\"mixtral-8x7b — generated by TensorCAD.\n\nDo not edit by hand: change the design and regenerate.\n\nSymbols:\n    L = 32\n    D = 4096\n    H = 32\n    Hkv = 8\n    dh = 128\n    F = 14336\n    V = 32000\n    E = 8\n    K = 2\n    Fe = 14336\n\nParameters: 46,702,792,704\n\"\"\"\n\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\nclass RotaryEmbedding(nn.Module):\n    \"\"\"Rotary position embedding over the last dimension of (B, H, T, head_dim).\"\"\"\n\n    def __init__(self, head_dim: int, theta: float = 10000.0):\n        super().__init__()\n        self.head_dim = head_dim\n        inv_freq = 1.0 / (theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32) / head_dim))\n        self.register_buffer(\"inv_freq\", inv_freq, persistent=False)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        seq = x.shape[-2]\n        pos = torch.arange(seq, device=x.device, dtype=torch.float32)\n        freqs = torch.outer(pos, self.inv_freq.to(x.device))\n        cos = freqs.cos().to(x.dtype)[None, None, :, :]\n        sin = freqs.sin().to(x.dtype)[None, None, :, :]\n        x1, x2 = x[..., : self.head_dim // 2], x[..., self.head_dim // 2 :]\n        return torch.cat([x1 * cos - x2 * sin, x1 * sin + x2 * cos], dim=-1)\n\n\nclass GqaAttention(nn.Module):\n    \"\"\"gqa_attention: d_model=4096, heads=32, kv_heads=8, head_dim=128, bias=false, causal=true, window=0, qk_norm=false, flash=true, logit_softcap=0, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.q_proj = nn.Linear(4096, 4096, bias=False)\n        self.k_proj = nn.Linear(4096, 1024, bias=False)\n        self.v_proj = nn.Linear(4096, 1024, bias=False)\n        self.rope_q = RotaryEmbedding(128, theta=1000000)\n        self.rope_k = RotaryEmbedding(128, theta=1000000)\n        self.o_proj = nn.Linear(4096, 4096, bias=False)\n\n    def forward(self, x):\n        q_proj_y = self.q_proj(x)\n        k_proj_y = self.k_proj(x)\n        v_proj_y = self.v_proj(x)\n        B, T, _d2 = q_proj_y.shape\n        q_heads_y = q_proj_y.view(B, T, 32, 128).permute(0, 2, 1, 3)\n        B, T, _d2 = k_proj_y.shape\n        k_heads_y = k_proj_y.view(B, T, 8, 128).permute(0, 2, 1, 3)\n        B, T, _d2 = v_proj_y.shape\n        v_heads_y = v_proj_y.view(B, T, 8, 128).permute(0, 2, 1, 3)\n        rope_q_y = self.rope_q(q_heads_y)\n        rope_k_y = self.rope_k(k_heads_y)\n        attn_y = F.scaled_dot_product_attention(rope_q_y, rope_k_y, v_heads_y, is_causal=True, enable_gqa=True)\n        B, _d1, T, _d3 = attn_y.shape\n        o_merge_y = attn_y.permute(0, 2, 1, 3).reshape(B, T, 4096)\n        o_proj_y = self.o_proj(o_merge_y)\n        return o_proj_y\n\nclass Expert(nn.Module):\n    \"\"\"Expert: d_model=4096, hidden=14336, act=silu, bias=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.gate = nn.Linear(4096, 14336, bias=False)\n        self.up = nn.Linear(4096, 14336, bias=False)\n        self.down = nn.Linear(14336, 4096, bias=False)\n\n    def forward(self, x):\n        gate_y = self.gate(x)\n        up_y = self.up(x)\n        act_y = F.silu(gate_y)\n        gated_y = act_y * up_y\n        down_y = self.down(gated_y)\n        return down_y\n\nclass MoeLayer(nn.Module):\n    \"\"\"8 experts, top 2 per token.\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.top_k = 2\n        self.router = nn.Linear(4096, 8, bias=False)\n        self.experts = nn.ModuleList([Expert() for _ in range(8)])\n\n    def forward(self, x):\n        shape = x.shape\n        flat = x.reshape(-1, 4096)\n        scores = F.softmax(self.router(flat).float(), dim=-1)\n        weight, index = torch.topk(scores, self.top_k, dim=-1)\n        weight = weight / weight.sum(dim=-1, keepdim=True)\n        weight = weight.to(x.dtype)\n        out = torch.zeros_like(flat)\n        for e, expert in enumerate(self.experts):\n            rows, slot = (index == e).nonzero(as_tuple=True)\n            if rows.numel() == 0:\n                continue\n            out.index_add_(0, rows, expert(flat[rows]) * weight[rows, slot, None])\n        y = out.reshape(shape)\n        return y\n\nclass TransformerBlock(nn.Module):\n    \"\"\"transformer_block: d_model=4096, heads=32, kv_heads=8, head_dim=128, ffn_hidden=14336, attention=gqa, q_lora=0, kv_lora=0, nope_dim=0, rope_dim=0, v_dim=0, norm=rmsnorm, norm_bias=true, post_norm=false, mlp=moe, experts=8, top_k=2, expert_hidden=14336, shared_experts=0, router_bias=false, act=silu, attn_bias=false, mlp_bias=false, qk_norm=false, causal=true, window=0, logit_softcap=0, value_embeddings=false\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.norm1 = nn.RMSNorm(4096, eps=0.00001)\n        self.attn = GqaAttention()\n        self.norm2 = nn.RMSNorm(4096, eps=0.00001)\n        self.mlp = MoeLayer()\n\n    def forward(self, x):\n        norm1_y = self.norm1(x)\n        attn_y = self.attn(norm1_y)\n        resid1_y = attn_y + x\n        norm2_y = self.norm2(resid1_y)\n        mlp_y = self.mlp(norm2_y)\n        resid2_y = mlp_y + resid1_y\n        return resid2_y\n\nclass Layer(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.block = TransformerBlock()\n\n    def forward(self, x):\n        block_y = self.block(x)\n        return block_y\n\nclass Net(nn.Module):\n\n    def __init__(self):\n        super().__init__()\n        self.embed = nn.Embedding(32000, 4096)\n        self.layers = nn.ModuleList([Layer() for _ in range(32)])\n        self.final_norm = nn.RMSNorm(4096, eps=0.00001)\n        self.head = nn.Linear(4096, 32000, bias=False)\n\n    def forward(self, ids):\n        embed_y = self.embed(ids)\n        layers_x = embed_y\n        for layer in self.layers:\n            layers_x = layer(layers_x)\n        final_norm_y = self.final_norm(layers_x)\n        head_y = self.head(final_norm_y)\n        return head_y\n"
    }
  ]
}
